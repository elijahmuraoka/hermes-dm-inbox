# Backlog

## Ideas

- Phase 0 scaffold: mock inbox, SQLite spine, keyboard UI, mock Hermes adapter
- iMessage read-only connector via `imsg`
- LinkedIn read-only connector via `linkedin-os`
- X/Twitter read-only connector via `xurl`
- Gmail read-only connector via `gog`
- Local Hermes adapter with explicit full-body opt-in
- Optional PIN auth for local browser access
- Explicit send queue with approval records (later phase only)
